% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-delta.R
\name{delta_classification}
\alias{delta_classification}
\title{Classify parameters from a two-stage delta analysis}
\usage{
delta_classification(result)
}
\arguments{
\item{result}{An \link{ospgsa_result} from \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}} (or \code{\link[=gsa]{gsa()}} with
\code{method = "delta"} and a correlation matrix).}
}
\value{
A \code{data.table} with one row per output / parameter giving \code{delta_1},
\code{delta_2}, their significance, and a \code{class} in
\verb{\{causal, indirect-only, both, non-influential\}}.
}
\description{
Classify parameters from a two-stage delta analysis
}
