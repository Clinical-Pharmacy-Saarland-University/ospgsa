% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-morris.R
\name{gsa_morris}
\alias{gsa_morris}
\title{Morris elementary-effects screening}
\usage{
gsa_morris(
  parameters,
  evaluator,
  r = 10L,
  levels = 4L,
  boot = 100L,
  conf = 0.95,
  log_output = FALSE,
  dummy = NULL
)
}
\arguments{
\item{parameters}{A \code{\link[=gsa_parameters]{gsa_parameters()}} object.}

\item{evaluator}{A model evaluator (see \code{\link[=function_evaluator]{function_evaluator()}},
\code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}).}

\item{r}{Number of trajectories (10-50 typical).}

\item{levels}{Number of grid levels \code{p} (even; default 4).}

\item{boot, conf}{Bootstrap resamples / confidence level for mu*.}

\item{log_output}{Analyse \code{log(Y)}.}

\item{dummy}{Optional phantom-parameter name for the significance floor.}
}
\value{
An \link{ospgsa_result} with \code{index} values \code{"mu_star"}, \code{"mu"}, \code{"sigma"}
and \code{"mu_star_norm"} (mu* normalized to sum to 1 within each output).
}
\description{
Computes the Morris screening measures mu (mean effect), mu* (mean absolute
effect, a robust importance ranking / proxy for the total effect) and sigma
(spread of effects, indicating nonlinearity and interactions), with bootstrap
CIs for mu* obtained by resampling trajectories.
}
\details{
Elementary effects are computed in the normalized unit-hypercube design space,
so the marginal distributions (including log-scaled parameters) are accounted
for through the inverse-CDF mapping and effects are comparable across
parameters. Use Morris as a cheap first pass to drop clearly inactive factors
before a quantitative analysis.
}
\examples{
p <- gsa_parameters(
  gsa_parameter("x1", "uniform", min = 0, max = 1),
  gsa_parameter("x2", "uniform", min = 0, max = 1),
  gsa_parameter("x3", "uniform", min = 0, max = 1))
f <- function(M) 3 * M[, 1] + 2 * M[, 2]               # x3 inert
gsa_morris(p, function_evaluator(f), r = 20)
}
\references{
Morris (1991) \emph{Technometrics} 33:161; Campolongo, Cariboni &
Saltelli (2007) \emph{Environ. Model. Softw.} 22:1509.
}
