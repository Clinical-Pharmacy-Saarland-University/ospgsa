% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{gsa_sanitize_positive}
\alias{gsa_sanitize_positive}
\title{Set non-positive outputs to NA for log-scale analysis}
\usage{
gsa_sanitize_positive(Y, quiet = FALSE)
}
\arguments{
\item{Y}{A numeric output matrix, or an evaluation result list with a \code{Y}.}

\item{quiet}{Suppress the count message.}
}
\value{
The same shape as \code{Y} with non-positive entries set to \code{NA} (and, for
an evaluation result, \code{n_failed} refreshed).
}
\description{
AUC / Cmax and similar PK metrics are analysed on the log scale
(\code{log_output = TRUE}), which is undefined for non-positive values. A stiff or
failed model run can occasionally return a zero or negative output; this
helper sets those to \code{NA} (the estimators drop \code{NA} rows) so a few bad values
do not contaminate the log transform. Accepts either an output matrix or an
evaluation result (the list returned by \code{\link[=gsa_evaluate]{gsa_evaluate()}}).
}
\examples{
Y <- matrix(c(1, -2, 3, 0), ncol = 1)
gsa_sanitize_positive(Y, quiet = TRUE)
}
\seealso{
\code{\link[=gsa_evaluate]{gsa_evaluate()}}
}
