% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{gsa_parameter}
\alias{gsa_parameter}
\title{Describe one uncertain model parameter}
\usage{
gsa_parameter(
  name,
  dist,
  ...,
  path = name,
  unit = NULL,
  scale = NULL,
  mol_weight = NULL,
  mol_weight_unit = "g/mol"
)
}
\arguments{
\item{name}{Short, unique label used in tables and plots.}

\item{dist}{Distribution: one of \code{"uniform"}, \code{"loguniform"}, \code{"normal"},
\code{"lognormal"}, \code{"truncnorm"}.}

\item{...}{Distribution arguments (in \strong{natural} parameter units):
\itemize{
\item \code{uniform}, \code{loguniform}: \code{min}, \code{max} (loguniform needs \code{min > 0}).
\item \code{normal}: \code{mean}, \code{sd}.
\item \code{lognormal}: one of \verb{(meanlog, sdlog)}, \verb{(median, gsd)} (geometric SD), or
\verb{(mean, cv)} (mean and coefficient of variation on the natural scale).
\item \code{truncnorm}: \code{mean}, \code{sd}, \code{lower}, \code{upper}.
}}

\item{path}{PK-Sim parameter path used by \code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}. Defaults to
\code{name}. Ignored by non-PK-Sim evaluators.}

\item{unit}{Optional display unit of the PK-Sim parameter. If supplied, sampled
values are converted to base units before being set (see \code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}).
Leave \code{NULL} to provide values already in base units.}

\item{scale}{Representation used for \strong{scale-dependent} estimators (Sobol,
Morris, regression): \code{"natural"} or \code{"log"}. Defaults to \code{"log"} for
\code{loguniform}/\code{lognormal} parameters and \code{"natural"} otherwise. The
moment-independent delta index is invariant to this choice. See the GSA
primer in the package repository for the log- vs linear-scale discussion.}

\item{mol_weight}{Optional molecular weight (used only for unit conversions of
amount/concentration parameters).}

\item{mol_weight_unit}{Unit of \code{mol_weight} (default \code{"g/mol"}).}
}
\value{
An object of class \code{gsa_parameter}.
}
\description{
Defines the marginal uncertainty distribution of a single model parameter for
global sensitivity analysis.
}
\examples{
gsa_parameter("Lipophilicity", "normal", mean = -0.1, sd = 0.3,
              path = "Aciclovir|Lipophilicity")
gsa_parameter("CL", "lognormal", median = 5, gsd = 1.5)
}
\seealso{
\code{\link[=gsa_parameters]{gsa_parameters()}}, \code{\link[=gsa_correlation]{gsa_correlation()}}
}
