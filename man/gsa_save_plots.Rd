% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{gsa_save_plots}
\alias{gsa_save_plots}
\title{Save the standard GSA plots as PNG files}
\usage{
gsa_save_plots(
  result,
  dir = ".",
  prefix = "plot_",
  width = 8,
  height = 5,
  dpi = 150,
  quiet = FALSE
)
}
\arguments{
\item{result}{An \link{ospgsa_result}.}

\item{dir}{Output directory (created if needed).}

\item{prefix}{File-name prefix.}

\item{width, height, dpi}{Passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{quiet}{Suppress the per-file progress messages.}
}
\value{
(Invisibly) the character vector of files written.
}
\description{
Writes one PNG per output for each index family present in \code{result}: the
two-stage delta split (when a \code{"full"} stage is present), \code{PRCC}/\code{SRC}
regression bars, Sobol \code{S1}/\code{ST}, and the Morris \verb{mu*} plane. Plots that
cannot be produced are skipped (not fatal). This packages the plot-writing
loop that GSA driver scripts otherwise repeat by hand.
}
\examples{
p <- gsa_parameters(
  gsa_parameter("a", "uniform", min = 0, max = 1),
  gsa_parameter("b", "uniform", min = 0, max = 1)
)
f <- function_evaluator(function(M) 2 * M[, "a"] + M[, "b"], "y")
res <- gsa(p, f, method = "sobol", n = 256, boot = 0)
gsa_save_plots(res, dir = tempfile("plots"))
}
\seealso{
\code{\link[=plot_delta_two_stage]{plot_delta_two_stage()}}, \code{\link[=plot_indices]{plot_indices()}}, \code{\link[=plot_sobol]{plot_sobol()}}
}
