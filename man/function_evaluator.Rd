% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{function_evaluator}
\alias{function_evaluator}
\title{Wrap an R function as a GSA model evaluator}
\usage{
function_evaluator(fun, output_names = NULL, vectorized = TRUE)
}
\arguments{
\item{fun}{A function taking a numeric matrix \code{X} (n x k, columns named by
parameter) and returning either a numeric vector of length \code{n} (single
output) or a numeric matrix \verb{n x m} (multiple outputs).}

\item{output_names}{Optional names for the outputs.}

\item{vectorized}{If \code{TRUE} (default) \code{fun} is called once on the whole
matrix; if \code{FALSE} it is applied row by row.}
}
\value{
A function with the evaluator contract, usable by \code{\link[=gsa_sobol]{gsa_sobol()}},
\code{\link[=gsa_morris]{gsa_morris()}} and \code{\link[=gsa]{gsa()}}.
}
\description{
Wrap an R function as a GSA model evaluator
}
\examples{
f <- function(M) sin(M[, 1]) + M[, 2]^2
ev <- function_evaluator(f, output_names = "y")
ev(matrix(runif(20), ncol = 2, dimnames = list(NULL, c("x1", "x2"))))
}
