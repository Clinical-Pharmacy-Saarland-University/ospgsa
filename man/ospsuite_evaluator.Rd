% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner-ospsuite.R
\name{ospsuite_evaluator}
\alias{ospsuite_evaluator}
\title{Build a PK-Sim (ospsuite) model evaluator}
\usage{
ospsuite_evaluator(
  simulation,
  parameters,
  outputs = NULL,
  pk_parameters = c("AUC_tEnd", "C_max", "t_max"),
  time_points = NULL,
  n_cores = 1L,
  output_names = NULL,
  silent = TRUE,
  chunk = 2000L,
  summary_fn = NULL
)
}
\arguments{
\item{simulation}{A loaded \code{ospsuite} \code{Simulation}, or a path to a \code{.pkml}
file.}

\item{parameters}{A \code{\link[=gsa_parameters]{gsa_parameters()}} object. Each non-dummy parameter's
\code{path} must resolve in the simulation.}

\item{outputs}{Output quantity path(s) to record. If \code{NULL}, the simulation's
existing output selection(s) are used.}

\item{pk_parameters}{Scalar PK parameters to extract (subset of
\code{ospsuite::allPKParameterNames()}), e.g. \code{c("AUC_tEnd", "C_max", "t_max")}.
Set to \code{character(0)} for time-resolved only.}

\item{time_points}{Optional numeric vector of times (in the simulation's base
time unit, minutes) at which to record concentrations (time-resolved GSA).}

\item{n_cores}{Number of cores used to solve the queued runs of the single
batch concurrently. Scaling plateaus near the number of physical cores
(hyperthreads add little for ODE solving).}

\item{output_names}{Optional named character vector mapping output paths to
short labels used in result column names.}

\item{silent}{Suppress the ospsuite run progress/among messages.}

\item{chunk}{Maximum number of rows queued and solved per inner sub-batch.
Bounds the number of \code{SimulationResults} held at once for very large single
calls; does not change which model is used (always the one warm model).}

\item{summary_fn}{Optional custom reducer, a function \verb{function(profiles, sr)}
that turns one simulated run into a named numeric vector of outputs. When
supplied it REPLACES the built-in \code{pk_parameters} / \code{time_points} extraction,
so you can compute any metric (\code{t_max}, a partial AUC, a slope, a ratio, time
above a threshold, ...). \code{profiles} is a named list (one per output) of
\code{data.frame(time, value)} in base units. \code{sr} is the raw \code{SimulationResults}
(use \code{ospsuite::calculatePKAnalyses(sr)} for PK-Sim's own PK parameters). The
returned names become the result columns and must be the same on every run.
Develop and debug it with \code{\link[=ospsuite_test_summary]{ospsuite_test_summary()}}.}
}
\value{
A function with the evaluator contract (matrix in -> matrix out).
}
\description{
Returns an evaluator that, given a matrix of parameter values, runs the
PK-Sim simulation once per row and returns scalar PK parameters and/or
time-resolved concentrations. The result plugs directly into \code{\link[=gsa]{gsa()}},
\code{\link[=gsa_sobol]{gsa_sobol()}}, \code{\link[=gsa_morris]{gsa_morris()}} and \code{\link[=gsa_evaluate]{gsa_evaluate()}}.
}
\details{
The evaluator loads \strong{one} compiled model and a \strong{single} \code{SimulationBatch},
and reuses them for every call: all rows are queued on that batch and solved
concurrently across \code{n_cores} (a single batch parallelises its queued runs
internally). Memory is therefore ~one model copy regardless of \code{n_cores}, and
there is no per-call reloading.

Values for parameters declared with a display \code{unit} are converted to base
units (with \code{ospsuite::toBaseUnit()}) before each run. Dummy parameters
(\code{\link[=gsa_dummy]{gsa_dummy()}}) are ignored by the model. Failed runs are returned as \code{NA}
rows, which the estimators drop.
}
\examples{
\dontrun{
sim <- ospsuite::loadSimulation(
  system.file("extdata", "Aciclovir.pkml", package = "ospsuite"))
p <- gsa_parameters(
  gsa_parameter("Lipophilicity", "normal", mean = -0.1, sd = 0.3,
                path = "Aciclovir|Lipophilicity"))
ev <- ospsuite_evaluator(sim, p, pk_parameters = c("AUC_tEnd", "C_max"))
ev(gsa_sample(p, 8)$X)
}
}
\seealso{
\code{\link[=gsa]{gsa()}}, \code{\link[=function_evaluator]{function_evaluator()}}, \code{\link[=ospsuite_test_summary]{ospsuite_test_summary()}}
}
