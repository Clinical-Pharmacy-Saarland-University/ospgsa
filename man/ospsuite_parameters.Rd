% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner-ospsuite.R
\name{ospsuite_parameters}
\alias{ospsuite_parameters}
\title{Build model-anchored GSA parameters from a specification table}
\usage{
ospsuite_parameters(simulation, specs, dummy = "dummy")
}
\arguments{
\item{simulation}{A loaded \code{ospsuite} \code{Simulation}, or a path to a \code{.pkml}.}

\item{specs}{A data frame (or list coercible to one) with columns \code{name},
\code{pattern}, \code{dist}, \code{width}, and optional \code{lower}, \code{upper} (used by
\code{truncnorm}).}

\item{dummy}{Name for an appended \code{\link[=gsa_dummy]{gsa_dummy()}} noise-floor parameter, or
\code{NULL}/\code{NA} to add none. Default \code{"dummy"}.}
}
\value{
A \code{\link[=gsa_parameters]{gsa_parameters()}} object, with the resolved table (name, dist,
nominal, path) attached as attribute \code{"resolved"}.
}
\description{
One-call setup of a \code{\link[=gsa_parameters]{gsa_parameters()}} object for a PK-Sim/MoBi model: it
resolves each parameter's path from a regex pattern, reads the model's current
value at that path (in base units) and \strong{anchors} the marginal there, and
(optionally) appends a dummy parameter. This replaces the hand-rolled
resolve/anchor/validate boilerplate of a typical driver script.
}
\details{
The marginal is anchored at the nominal value \code{m} per \code{dist}:
\itemize{
\item \code{lognormal} -> \code{median = m}, \code{gsd = width} (geometric SD),
\item \code{truncnorm} -> \code{mean = m}, \code{sd = width * m} (so \code{width} is a CV), with
\code{lower}/\code{upper} from the table,
\item \code{normal} -> \code{mean = m}, \code{sd = width} (absolute).
}

Values are taken in base units (\code{unit = NULL}), matching the values read from
the model, so no unit conversion is applied. Lognormal anchors must be
strictly positive (checked).
}
\examples{
\dontrun{
sim <- system.file("extdata", "Aciclovir.pkml", package = "ospsuite")
specs <- data.frame(
  name    = c("Lipophilicity", "FractionUnbound"),
  pattern = c("Aciclovir\\\\|Lipophilicity$",
              "Aciclovir\\\\|Fraction unbound \\\\(plasma\\\\)$"),
  dist    = c("normal", "truncnorm"),
  width   = c(0.3, 0.15),
  lower   = c(NA, 1e-4), upper = c(NA, 0.999)
)
params <- ospsuite_parameters(sim, specs)
}
}
\seealso{
\code{\link[=ospsuite_resolve_paths]{ospsuite_resolve_paths()}}, \code{\link[=gsa_parameter]{gsa_parameter()}}, \code{\link[=gsa_correlation]{gsa_correlation()}}
}
