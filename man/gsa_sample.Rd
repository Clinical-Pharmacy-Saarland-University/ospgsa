% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{gsa_sample}
\alias{gsa_sample}
\title{Draw a correlated / uncorrelated input sample}
\usage{
gsa_sample(
  parameters,
  n,
  method = c("lhs", "random"),
  correlation = NULL,
  correlation_method = c("iman-conover", "gaussian"),
  corr_type = c("spearman", "pearson"),
  seed = NULL
)
}
\arguments{
\item{parameters}{A \code{\link[=gsa_parameters]{gsa_parameters()}} object.}

\item{n}{Sample size.}

\item{method}{\code{"lhs"} (Latin hypercube, default) or \code{"random"}.}

\item{correlation}{Optional correlation matrix from \code{\link[=gsa_correlation]{gsa_correlation()}}. If
\code{NULL}, inputs are sampled independently.}

\item{correlation_method}{How a non-\code{NULL} correlation is induced:
\code{"iman-conover"} (default; targets the \strong{rank} correlation and preserves
marginals/LHS exactly) or \code{"gaussian"} (Gaussian copula / NORTA via the
normal scale).}

\item{corr_type}{Interpretation of \code{correlation}: \code{"spearman"} (rank, default)
or \code{"pearson"}. For the Gaussian copula a Spearman target is mapped to the
latent normal correlation via \code{rho_Z = 2 * sin(pi * rho_S / 6)}; Iman-Conover
always induces a rank correlation.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
An object of class \code{gsa_sample}: a list with \code{X} (an \verb{n x k} numeric
matrix in natural units, columns named by parameter), plus the originating
\code{parameters}, \code{correlation} and method metadata.
}
\description{
Generates a Monte Carlo design in natural parameter units, honouring each
marginal distribution and an optional correlation structure. This sample is
the basis for the moment-independent delta index and the regression methods.
}
\examples{
p <- gsa_parameters(
  gsa_parameter("a", "uniform", min = 0, max = 1),
  gsa_parameter("b", "lognormal", median = 2, gsd = 1.4)
)
s <- gsa_sample(p, n = 200, correlation = gsa_correlation(p, c("a", "b", 0.6)))
stats::cor(s$X, method = "spearman")
}
