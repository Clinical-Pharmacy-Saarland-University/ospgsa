% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-delta.R
\name{ospgsa_cluster}
\alias{ospgsa_cluster}
\title{Create a parallel cluster with ospgsa loaded on the workers}
\usage{
ospgsa_cluster(n_cores, pkg_dir = NULL, libpaths = .libPaths())
}
\arguments{
\item{n_cores}{Number of workers.}

\item{pkg_dir}{Optional path to the ospgsa \strong{source} tree; when given, workers
load it with \code{pkgload::load_all()} (for a not-installed / development
package). When \code{NULL}, workers run \code{library(ospgsa)} (installed package).}

\item{libpaths}{Library paths to prepend on each worker (e.g. a non-default
user library). Defaults to the current \code{.libPaths()}.}
}
\value{
A running PSOCK cluster. Stop it with \code{\link[parallel:stopCluster]{parallel::stopCluster()}} when
done (e.g. via \code{on.exit()}).
}
\description{
Convenience wrapper for parallel GSA: builds a PSOCK cluster (works on
Windows; no \code{fork}) and \strong{silently} loads ospgsa on every worker, so it can
be passed as \code{cl} to \code{\link[=gsa_delta]{gsa_delta()}}, \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}} or \code{\link[=gsa]{gsa()}}. Use it
instead of a hand-rolled \code{parallel::clusterCall()}, which prints its
per-worker return value at the top level of a script.
}
\examples{
\dontrun{
cl <- ospgsa_cluster(8, pkg_dir = "path/to/ospgsa")
on.exit(parallel::stopCluster(cl))
res <- gsa_delta_two_stage(Xf, Yf, Xi, Yi, seed = 1, cl = cl)
}
}
