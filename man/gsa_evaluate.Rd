% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{gsa_evaluate}
\alias{gsa_evaluate}
\title{Evaluate a design with an evaluator (optionally checkpointed and resumable)}
\usage{
gsa_evaluate(
  X,
  evaluator,
  checkpoint_dir = NULL,
  block_size = 1000L,
  tag = "block",
  crash_skip = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{X}{A \code{\link[=gsa_sample]{gsa_sample()}} or a numeric input matrix (natural units).}

\item{evaluator}{A model evaluator (see \code{\link[=function_evaluator]{function_evaluator()}},
\code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}).}

\item{checkpoint_dir}{Optional path. When \code{NULL} (default) the whole design is
evaluated in a single call (no checkpointing). When a directory is given the
evaluation is blocked and resumable; the directory is created if needed.}

\item{block_size}{Rows evaluated and checkpointed per block (only used when
\code{checkpoint_dir} is set). Smaller blocks checkpoint more often -- less work
lost on a crash -- at negligible overhead.}

\item{tag}{Short prefix for this design's checkpoint files. Use distinct tags
to keep several designs' checkpoints (e.g. an independent and a correlated
design) side by side in one directory.}

\item{crash_skip}{If \code{TRUE}, guard each block with a marker file so that a
block which \emph{hard-crashes} the R process (e.g. a solver segfault on a stiff
parameter draw, which cannot be caught with \code{\link[=tryCatch]{tryCatch()}}) is filled with
\code{NA} rows on the next resume instead of crashing again -- the estimators
drop \code{NA} rows. Off by default because, while on, interrupting a block
mid-evaluation also turns that block into \code{NA} on resume. Turn it on for
unattended runs of models that crash on some draws, and re-run until
completion (e.g. in a loop / watchdog). Requires \code{checkpoint_dir}.}

\item{quiet}{Suppress per-block progress messages.}
}
\value{
A list with \code{X} (the input matrix), \code{Y} (the output matrix) and
\code{n_failed} (rows with any non-finite output). When \code{checkpoint_dir} is set
it additionally carries \code{n_blocks}, \code{checkpoint_dir}, \code{tag} and \code{key} (the
design content hash used in the checkpoint file names).
}
\description{
Runs a design matrix through a model evaluator (see \code{\link[=function_evaluator]{function_evaluator()}},
\code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}) and collects the outputs.
}
\details{
For long or fragile runs -- thousands of PK-Sim solves that can take hours --
pass \code{checkpoint_dir} to evaluate the design in \strong{resumable blocks}. Each
block of \code{block_size} rows is written to disk as soon as it finishes, keyed by
a content hash of the design; a re-run loads the completed blocks instead of
recomputing them. A crash, an interruption, or a deliberate stop therefore
costs at most one block, and re-running the same call picks up where it left
off. This is the engine the bundled \code{gsa.R} driver scripts use.
}
\examples{
f <- function_evaluator(function(M) M[, 1] + M[, 2]^2, "y")
X <- matrix(runif(20), ncol = 2, dimnames = list(NULL, c("a", "b")))
gsa_evaluate(X, f)$Y

# Resumable, crash-safe evaluation of a long run. A second call with the same
# design and directory loads the finished blocks instead of recomputing them.
dir <- tempfile("ckpt")
r1 <- gsa_evaluate(X, f, checkpoint_dir = dir, block_size = 5, tag = "demo")
r2 <- gsa_evaluate(X, f, checkpoint_dir = dir, block_size = 5, tag = "demo")
identical(r1$Y, r2$Y)
}
\seealso{
\code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}, \code{\link[=gsa]{gsa()}}
}
