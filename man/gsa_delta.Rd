% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-delta.R
\name{gsa_delta}
\alias{gsa_delta}
\title{Borgonovo delta moment-independent sensitivity index}
\usage{
gsa_delta(
  X,
  Y,
  boot = 100L,
  conf = 0.95,
  ci = c("percentile", "normal"),
  min_per_class = 10L,
  grid_n = 110L,
  include_s1 = TRUE,
  log_output = FALSE,
  classes = NULL,
  dummy = NULL,
  seed = NULL,
  n_cores = 1L,
  cl = NULL
)
}
\arguments{
\item{X}{Input design: a \code{\link[=gsa_sample]{gsa_sample()}}, a numeric matrix, or a data.frame
(one column per parameter, in natural units). Column names are the parameter
labels.}

\item{Y}{Model output(s): a numeric vector (single output), a matrix /
data.frame (one column per output), or an evaluation result from
\code{\link[=gsa_evaluate]{gsa_evaluate()}}. Failed runs encoded as \code{NA} are dropped per output.}

\item{boot}{Number of bootstrap resamples for bias correction and CIs (0 to
skip). Use >= 500 for publication.}

\item{conf}{Confidence level for the bootstrap interval.}

\item{ci}{Bootstrap interval type: \code{"percentile"} (default, recommended for
this bounded, near-zero-skewed index) or \code{"normal"}.}

\item{min_per_class}{Minimum number of points required in a rank class for its
conditional KDE to be used.}

\item{grid_n}{Number of output grid points for the KDE integration.}

\item{include_s1}{Also report the first-order Sobol index obtained for free
from the same rank partition (a cross-check; no CI).}

\item{log_output}{Analyse \code{log(Y)} instead of \code{Y} (recommended for strongly
right-skewed metrics such as AUC and Cmax). Requires positive outputs.}

\item{classes}{Optional fixed number of rank classes (overrides the automatic
rule).}

\item{dummy}{Optional name of a phantom parameter used as the significance
noise floor (see \code{\link[=gsa_parameters]{gsa_parameters()}} and \code{\link[=gsa_dummy]{gsa_dummy()}}).}

\item{seed}{Optional integer. When supplied (or whenever \code{n_cores > 1}), each
(output, parameter) task's bootstrap is run on an independent
L'Ecuyer-CMRG substream derived from \code{seed}, so results are reproducible
\strong{and identical whether computed serially or in parallel}. When \code{NULL} and
\code{n_cores == 1}, the ambient RNG stream is used (unchanged legacy behaviour).}

\item{n_cores}{Number of workers for evaluating the (output x parameter)
tasks in parallel. \code{1} (default) runs serially. Parallelism uses a PSOCK
cluster (works on Windows; no \code{fork}).}

\item{cl}{Optional pre-built \code{parallel} cluster to reuse (its workers must
have ospgsa available, e.g. an installed package or one loaded via
\code{pkgload::load_all} on each worker). Overrides \code{n_cores}; not stopped by
this function. Useful to share one cluster across both delta stages.}
}
\value{
An \link{ospgsa_result}.
}
\description{
Estimates the delta index from a single input-output sample using the
given-data estimator (Plischke, Borgonovo and Smith, 2013): equal-frequency
rank classes, Gaussian KDE of the unconditional and class-conditional output
densities, half-L1 shift weighted by class probability, bootstrap bias
correction and bootstrap confidence intervals.
}
\details{
The delta index is \strong{moment-independent} (it responds to changes in any
feature of the output distribution, not only its variance) and is \strong{well
defined under correlated inputs}, which makes it the recommended index for
PBPK models. See \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}} to separate a parameter's structural
effect from the part it inherits through correlation.
}
\examples{
set.seed(1)
X <- matrix(runif(2000 * 3), ncol = 3, dimnames = list(NULL, c("a", "b", "c")))
y <- 3 * X[, 1] + X[, 2]                 # c is inert
gsa_delta(X, y, boot = 50)
}
\references{
Borgonovo E (2007) \emph{Reliab Eng Syst Saf} 92(6):771-784. \doi{10.1016/j.ress.2006.04.015}.
Plischke E, Borgonovo E, Smith CL (2013) \emph{Eur J Oper Res} 226(3):536-550. \doi{10.1016/j.ejor.2012.11.047}.
}
\seealso{
\code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}}, \code{\link[=gsa_sobol]{gsa_sobol()}}
}
