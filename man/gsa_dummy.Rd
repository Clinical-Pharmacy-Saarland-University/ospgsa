% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{gsa_dummy}
\alias{gsa_dummy}
\title{Add a phantom ("dummy") parameter as a significance noise floor}
\usage{
gsa_dummy(name = "dummy")
}
\arguments{
\item{name}{Label for the dummy parameter (default \code{"dummy"}).}
}
\value{
A \code{gsa_parameter} flagged as a dummy (uniform on \verb{[0, 1]}).
}
\description{
A dummy parameter is sampled like any other input but is \strong{not connected to
the model} (PK-Sim evaluators skip it). Its estimated sensitivity reflects
pure Monte Carlo / estimator noise, giving an empirical significance floor:
any real parameter whose index is indistinguishable from the dummy's is not
meaningfully influential. Pass the dummy's \code{name} as the \code{dummy} argument to
the estimators / \code{\link[=gsa]{gsa()}}.
}
\examples{
gsa_parameters(
  gsa_parameter("a", "uniform", min = 0, max = 1),
  gsa_dummy()
)
}
