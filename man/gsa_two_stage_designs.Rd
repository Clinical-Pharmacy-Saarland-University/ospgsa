% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling.R
\name{gsa_two_stage_designs}
\alias{gsa_two_stage_designs}
\title{Build the design pair for a two-stage delta analysis}
\usage{
gsa_two_stage_designs(
  parameters,
  n,
  correlation,
  seed = NULL,
  method = c("lhs", "random"),
  correlation_method = c("iman-conover", "gaussian"),
  corr_type = c("spearman", "pearson")
)
}
\arguments{
\item{parameters}{A \code{\link[=gsa_parameters]{gsa_parameters()}} object.}

\item{n}{Design size per stage.}

\item{correlation}{A correlation matrix from \code{\link[=gsa_correlation]{gsa_correlation()}} for the
correlated (Stage 2) design.}

\item{seed}{Optional integer seed for the correlated design; the independent
design uses \code{seed + 1}.}

\item{method, correlation_method, corr_type}{Passed to \code{\link[=gsa_sample]{gsa_sample()}}.}
}
\value{
A list with two \code{\link[=gsa_sample]{gsa_sample()}} objects: \code{full} (correlated, Stage 2)
and \code{ind} (independent, Stage 1).
}
\description{
Draws the two designs that \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}} compares: an \strong{independent}
(Stage 1) design from the marginals alone, and a \strong{correlated} (Stage 2)
design from the joint distribution. The two use \emph{distinct} seeds (\code{seed} for
the correlated design, \code{seed + 1} for the independent one) so their Monte-Carlo
errors are independent -- the convention required for the \code{delta_2 - delta_1}
contrast and the dummy noise floor. The returned designs are ready to feed to
\code{\link[=gsa_evaluate]{gsa_evaluate()}} (e.g. with \verb{checkpoint_dir =} for long runs).
}
\examples{
p <- gsa_parameters(
  gsa_parameter("a", "uniform", min = 0, max = 1),
  gsa_parameter("b", "uniform", min = 0, max = 1)
)
R <- gsa_correlation(p, c("a", "b", 0.6))
d <- gsa_two_stage_designs(p, 200, correlation = R, seed = 1)
dim(d$full$X)
}
\seealso{
\code{\link[=gsa_evaluate]{gsa_evaluate()}}, \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}}
}
