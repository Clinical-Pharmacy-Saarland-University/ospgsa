% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_delta_two_stage}
\alias{plot_delta_two_stage}
\title{Two-stage delta plot (independent vs full)}
\usage{
plot_delta_two_stage(
  result,
  output = NULL,
  labels = NULL,
  show_values = FALSE,
  digits = 3L
)
}
\arguments{
\item{result}{An \link{ospgsa_result} from \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}} or \code{\link[=gsa]{gsa()}}.}

\item{output}{Optional output filter.}

\item{labels}{Optional named character vector mapping parameter names (as in
\code{result$indices$parameter}) to display labels for the axis (e.g. publication
names). Parameters absent from the map keep their original name.}

\item{show_values}{If \code{TRUE}, print each bar's delta estimate at the bar end
(matching a local-SA style figure).}

\item{digits}{Decimal places for the printed values.}
}
\value{
A \code{ggplot} object comparing delta_1 (structural) and delta_2 (full).
}
\description{
Two-stage delta plot (independent vs full)
}
