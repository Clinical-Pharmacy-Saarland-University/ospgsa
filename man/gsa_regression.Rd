% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-regression.R
\name{gsa_regression}
\alias{gsa_regression}
\title{Regression / partial-correlation sensitivity indices}
\usage{
gsa_regression(
  X,
  Y,
  methods = c("SRC", "PRCC"),
  parameters = NULL,
  boot = 100L,
  conf = 0.95,
  log_output = FALSE,
  dummy = NULL
)
}
\arguments{
\item{X}{Input design (\code{\link[=gsa_sample]{gsa_sample()}}, matrix or data.frame). For SRC, columns
are transformed to the per-parameter estimator scale (e.g. log) when a
\code{\link[=gsa_sample]{gsa_sample()}} / \code{parameters} is supplied.}

\item{Y}{Output(s) (vector, matrix, data.frame or evaluation result).}

\item{methods}{Which indices to compute: any of \code{"SRC"}, \code{"SRRC"}, \code{"PCC"},
\code{"PRCC"}. Default \code{c("SRC", "PRCC")}.}

\item{parameters}{Optional \code{\link[=gsa_parameters]{gsa_parameters()}} used to apply per-parameter
scale transforms for SRC (ignored for rank methods).}

\item{boot, conf}{Bootstrap resamples / confidence level.}

\item{log_output}{Analyse \code{log(Y)}.}

\item{dummy}{Optional phantom-parameter name for the significance floor.}
}
\value{
An \link{ospgsa_result}; \code{index} is one of the requested methods plus an
\code{"R2"} row (parameter \code{"__model__"}) per output and regression method.
}
\description{
Computes standardized regression coefficients (SRC / SRRC) and partial
correlation coefficients (PCC / PRCC) from a single input-output sample, with
bootstrap confidence intervals and the model \code{R^2} (SRC / SRRC are only
reliable when \code{R^2} is high; escalate to \code{\link[=gsa_delta]{gsa_delta()}} / \code{\link[=gsa_sobol]{gsa_sobol()}} for
non-(rank-)linear models).
}
\examples{
set.seed(1)
X <- matrix(runif(1000 * 3), ncol = 3, dimnames = list(NULL, c("a", "b", "c")))
y <- 2 * X[, 1] - X[, 2]
gsa_regression(X, y, boot = 50)
}
\references{
Marino et al. (2008) \emph{J. Theor. Biol.} 254:178 (PRCC); Saltelli et
al. (2008) \emph{Global Sensitivity Analysis: The Primer}.
}
