% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-sobol.R
\name{gsa_sobol}
\alias{gsa_sobol}
\title{Sobol first- and total-order sensitivity indices}
\usage{
gsa_sobol(
  parameters,
  evaluator,
  n = 1024L,
  boot = 100L,
  conf = 0.95,
  method = c("lhs", "random"),
  log_output = FALSE,
  dummy = NULL,
  correlation = NULL
)
}
\arguments{
\item{parameters}{A \code{\link[=gsa_parameters]{gsa_parameters()}} object.}

\item{evaluator}{A model evaluator: a function mapping a natural-unit input
matrix (columns named by parameter) to an output matrix (see
\code{\link[=function_evaluator]{function_evaluator()}}, \code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}}).}

\item{n}{Base sample size (a power of two is conventional).}

\item{boot, conf}{Bootstrap resamples and confidence level.}

\item{method}{Marginal sampling: \code{"lhs"} (default) or \code{"random"}.}

\item{log_output}{Analyse \code{log(Y)} (recommended for AUC / Cmax).}

\item{dummy}{Optional phantom-parameter name used as the significance floor.}

\item{correlation}{Ignored (with a warning) if non-\code{NULL}.}
}
\value{
An \link{ospgsa_result} with \code{index} values \code{"S1"} and \code{"ST"}.
}
\description{
Variance-based indices via the pick-freeze design (Saltelli 2010 first-order,
Jansen 1999 total-order) with bootstrap confidence intervals. Total model
evaluations: \code{n * (k + 2)}.
}
\details{
\strong{Independence assumption.} Sobol indices are only interpretable for
independent inputs; under correlation \code{sum(Si)} need not be <= 1 and a
non-influential parameter can appear important. For correlated PBPK parameters
use \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}}. If \code{correlation} is supplied here it is ignored
with a warning (the design is built from the marginals).
}
\examples{
p <- gsa_parameters(
  gsa_parameter("x1", "uniform", min = -pi, max = pi),
  gsa_parameter("x2", "uniform", min = -pi, max = pi),
  gsa_parameter("x3", "uniform", min = -pi, max = pi))
ishigami <- function(M) sin(M[, 1]) + 7 * sin(M[, 2])^2 + 0.1 * M[, 3]^4 * sin(M[, 1])
gsa_sobol(p, function_evaluator(ishigami), n = 2048, boot = 50)
}
\references{
Saltelli et al. (2010) \emph{Comput. Phys. Commun.} 181:259;
Jansen (1999) \emph{Comput. Phys. Commun.} 117:35.
}
