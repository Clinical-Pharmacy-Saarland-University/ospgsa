% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner-ospsuite.R
\name{ospsuite_parameter_paths}
\alias{ospsuite_parameter_paths}
\title{List or search the parameter paths of an OSP simulation}
\usage{
ospsuite_parameter_paths(
  simulation,
  pattern = NULL,
  ignore_case = TRUE,
  fixed = FALSE,
  value = FALSE,
  max = Inf
)
}
\arguments{
\item{simulation}{A loaded \code{ospsuite} \code{Simulation}, or a path to a \code{.pkml}.}

\item{pattern}{Optional search string. \code{NULL} (default) returns every path.}

\item{ignore_case}{Match case-insensitively (default \code{TRUE}).}

\item{fixed}{If \code{TRUE}, treat \code{pattern} as a literal substring (no regex), so
you do not have to escape the \code{|} path separator. Default \code{FALSE} (PCRE).}

\item{value}{If \code{TRUE}, return a data frame of \code{path} and current \code{value}
(base units) rather than a character vector.}

\item{max}{Maximum number of paths to return (default \code{Inf}).}
}
\value{
A sorted character vector of matching paths, or, when \code{value = TRUE},
a data frame with columns \code{path} and \code{value}.
}
\description{
Discovery aid for finding a parameter's path in a PK-Sim/MoBi model. Returns
the absolute parameter paths, optionally filtered by a pattern and optionally
with each path's current value. Use it to find the path (or build the regex)
that \code{\link[=ospsuite_parameters]{ospsuite_parameters()}} / \code{\link[=ospsuite_resolve_paths]{ospsuite_resolve_paths()}} need, instead of
reaching for raw \code{ospsuite::getAllParameterPathsIn()} plus \code{grep()}.
}
\examples{
\dontrun{
sim <- system.file("extdata", "Aciclovir.pkml", package = "ospsuite")
ospsuite_parameter_paths(sim, "lipophilicity")
ospsuite_parameter_paths(sim, "Fraction unbound", fixed = TRUE, value = TRUE)
}
}
\seealso{
\code{\link[=ospsuite_resolve_paths]{ospsuite_resolve_paths()}}, \code{\link[=ospsuite_parameters]{ospsuite_parameters()}}
}
