% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsa.R
\name{gsa}
\alias{gsa}
\title{Run a global sensitivity analysis}
\usage{
gsa(
  parameters,
  evaluator,
  method = c("delta", "sobol", "morris", "regression"),
  n = NULL,
  correlation = NULL,
  corr_type = "spearman",
  correlation_method = "iman-conover",
  boot = 100L,
  conf = 0.95,
  ci = "percentile",
  log_output = FALSE,
  add_dummy = TRUE,
  dummy_name = "dummy",
  seed = NULL,
  r = 10L,
  levels = 4L,
  reg_methods = c("SRC", "PRCC"),
  n_cores = 1L
)
}
\arguments{
\item{parameters}{A \code{\link[=gsa_parameters]{gsa_parameters()}} object.}

\item{evaluator}{A model evaluator (\code{\link[=ospsuite_evaluator]{ospsuite_evaluator()}} for PK-Sim, or
\code{\link[=function_evaluator]{function_evaluator()}}).}

\item{method}{One or more of \code{"delta"} (default, recommended for PBPK),
\code{"sobol"}, \code{"morris"}, \code{"regression"}.}

\item{n}{Base sample size for \code{delta}/\code{regression}/\code{sobol}. Defaults to 2000
(given-data) or 1024 (sobol).}

\item{correlation}{Optional correlation matrix (\code{\link[=gsa_correlation]{gsa_correlation()}}). Triggers
two-stage delta; ignored by sobol/morris (with a warning).}

\item{corr_type, correlation_method}{Passed to \code{\link[=gsa_sample]{gsa_sample()}}.}

\item{boot, conf, ci}{Bootstrap settings.}

\item{log_output}{Analyse \code{log(Y)} (recommended for AUC / Cmax).}

\item{add_dummy, dummy_name}{Add a phantom parameter as the significance floor.}

\item{seed}{Optional RNG seed.}

\item{r, levels}{Morris trajectory count and grid levels.}

\item{reg_methods}{Regression indices to compute (see \code{\link[=gsa_regression]{gsa_regression()}}).}

\item{n_cores}{Workers for the delta estimator's per-task bootstrap (passed to
\code{\link[=gsa_delta]{gsa_delta()}} / \code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}}); \code{1} (default) is serial. With a
source package, parallel workers must load it (see \code{\link[=gsa_delta]{gsa_delta()}} \code{cl}).}
}
\value{
An \link{ospgsa_result} (combined across methods if several are requested).
}
\description{
One entry point that builds the appropriate design(s), evaluates the model and
computes the requested sensitivity indices. For correlated inputs with
\code{method = "delta"} it automatically runs the \strong{two-stage} analysis
(independent + correlated designs). A dummy noise-floor parameter is added by
default so significance is calibrated empirically.
}
\examples{
p <- gsa_parameters(
  gsa_parameter("x1", "uniform", min = 0, max = 1),
  gsa_parameter("x2", "uniform", min = 0, max = 1),
  gsa_parameter("x3", "uniform", min = 0, max = 1))
ev <- function_evaluator(function(M) 3 * M[, 1] + M[, 2], "y")
gsa(p, ev, method = "delta", n = 1500, boot = 40, seed = 1)
}
\seealso{
\code{\link[=gsa_delta_two_stage]{gsa_delta_two_stage()}}, \code{\link[=gsa_sobol]{gsa_sobol()}}, \code{\link[=gsa_morris]{gsa_morris()}},
\code{\link[=gsa_regression]{gsa_regression()}}, \code{\link[=gsa_convergence]{gsa_convergence()}}
}
