% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsa.R
\name{gsa_convergence}
\alias{gsa_convergence}
\title{Convergence of GSA indices with sample size}
\usage{
gsa_convergence(
  parameters,
  evaluator,
  method = c("sobol", "delta", "regression"),
  n_seq = c(256L, 512L, 1024L, 2048L),
  boot = 50L,
  conf = 0.95,
  log_output = FALSE,
  seed = NULL,
  index = NULL
)
}
\arguments{
\item{parameters, evaluator}{As in \code{\link[=gsa]{gsa()}}.}

\item{method}{One of \code{"delta"}, \code{"sobol"}, \code{"regression"} (given-data /
variance methods).}

\item{n_seq}{Increasing sample sizes.}

\item{boot, conf, log_output, seed}{As in \code{\link[=gsa]{gsa()}}.}

\item{index}{Which index to track (default depends on method: \code{"delta"},
\code{"ST"} or \code{"PRCC"}).}
}
\value{
A \code{data.table} (class \code{ospgsa_convergence}) with columns \code{n},
\code{output}, \code{parameter}, \code{index}, \code{estimate}, \code{conf_low}, \code{conf_high}.
}
\description{
Re-estimates the indices at increasing sample sizes to check Monte Carlo
convergence (stable rankings, tight CIs).
}
