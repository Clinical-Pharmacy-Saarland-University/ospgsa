% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators-delta.R
\name{gsa_delta_two_stage}
\alias{gsa_delta_two_stage}
\title{Two-stage delta analysis for correlated inputs}
\usage{
gsa_delta_two_stage(
  X_full,
  Y_full,
  X_ind,
  Y_ind,
  boot = 100L,
  conf = 0.95,
  ci = c("percentile", "normal"),
  min_per_class = 10L,
  grid_n = 110L,
  log_output = FALSE,
  classes = NULL,
  dummy = NULL,
  seed = NULL,
  n_cores = 1L,
  cl = NULL
)
}
\arguments{
\item{X_full, Y_full}{Stage-2 correlated design and its outputs.}

\item{X_ind, Y_ind}{Stage-1 independent design and its outputs.}

\item{boot, conf, ci, min_per_class, grid_n, log_output, classes, dummy}{Passed to
\code{\link[=gsa_delta]{gsa_delta()}}.}

\item{seed, n_cores, cl}{Bootstrap parallelism, passed to \code{\link[=gsa_delta]{gsa_delta()}} for each
stage. The two stages use distinct (offset) seeds so their bootstraps are
independent; results are reproducible and identical to the serial path. A
single cluster is built (or the supplied \code{cl} reused) across both stages.}
}
\value{
An \link{ospgsa_result} whose \code{indices} contains \code{stage} values
\code{"independent"} (delta_1), \code{"full"} (delta_2) and \code{"correlative"}
(delta_2 - delta_1, an attribution heuristic; delta is not additive).
}
\description{
Computes the delta index on two designs and compares them to separate each
parameter's \strong{structural / causal} effect from the effect it inherits through
\strong{correlation} with other parameters (De Carlo et al. 2023; adapted to PBPK
by Cuquerella-Gilabert et al. 2026):
}
\details{
\itemize{
\item \strong{Stage 1 (independent):} parameters sampled from their marginals with all
correlations switched off -> \code{delta_1}. A non-zero \code{delta_1} certifies a
genuine direct effect (delta = 0 iff Y is independent of the parameter \emph{and}
the parameter is uncorrelated with the others).
\item \strong{Stage 2 (full):} parameters sampled from the realistic correlated joint
distribution -> \code{delta_2} (importance under the true output distribution).
}

Each parameter is then classified as \emph{causal}, \emph{indirect only} (influential
purely via correlation) or \emph{both} (see \code{\link[=delta_classification]{delta_classification()}}).
}
\examples{
set.seed(1)
p <- gsa_parameters(
  gsa_parameter("k2",   "uniform", min = 0, max = 1),
  gsa_parameter("IC50", "uniform", min = 0, max = 1)  # inert but correlated to k2
)
R  <- gsa_correlation(p, c("k2", "IC50", 0.9))
Sf <- gsa_sample(p, 2000, correlation = R, seed = 1)
Si <- gsa_sample(p, 2000, seed = 1)
f  <- function(M) 2 * M[, "k2"]                       # only k2 acts
gsa_delta_two_stage(Sf$X, f(Sf$X), Si$X, f(Si$X), boot = 50)
}
\seealso{
\code{\link[=delta_classification]{delta_classification()}}, \code{\link[=gsa]{gsa()}}
}
